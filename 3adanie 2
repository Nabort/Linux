#!/bin/bash
# Проверяем, что передан один аргумент
if [ $# -ne 1 ]; then
  echo "Использование: $0 число"
  exit 1
fi

# Проверяем четность
if [ $(($1 % 2)) -eq 0 ]; then
  echo "$1 является четным числом."
else
  echo "$1 является нечетным числом."
fi
