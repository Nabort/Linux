#!/bin/bash
# Проверяем, что передан путь к директории
if [ $# -ne 1 ]; then
  echo "Использование: $0 директория"
  exit 1
fi

# Удаляем файлы старше 5 дней
find "$1" -type f -mtime +5 -exec rm {} \;
